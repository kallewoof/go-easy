/**
 * Calendar module — stub.
 * Implementation phase 3 (after Drive).
 */

export type { CalendarEvent, Attendee, CalendarInfo, FreeBusySlot } from './types.js';

// TODO: listCalendars, listEvents, getEvent, createEvent,
//       updateEvent, deleteEvent, queryFreeBusy
