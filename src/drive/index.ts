/**
 * Drive module — stub.
 * Implementation phase 2 (after Gmail).
 */

export type { DriveFile, DrivePermission, ExportFormat } from './types.js';

// TODO: listFiles, search, download, upload, exportFile,
//       move, rename, copy, trash, share, unshare,
//       listSharedDrives, searchInDrive
